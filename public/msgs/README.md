# gab-grad-dedications

## Compiler

This folder contains `compiler.py` that compiles all messages into one JSON file. For any changes in the messages, run:

```bash
python compiler.py > msgs.json
```
